import { QuestionDropdown } from '../../classes/forms/question-dropdown';

describe('QuestionDropdown', () => {
  it('should create an instance', () => {
    expect(new QuestionDropdown()).toBeTruthy();
  });
});
